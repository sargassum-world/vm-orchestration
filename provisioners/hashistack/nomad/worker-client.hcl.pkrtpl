client {
  enabled = true
  servers = ["127.0.0.1"]

  server_join {
    retry_join = ["provider=gce tag_key=${nomad_server_tag}"]
  }
}

plugin "docker" {
  config {
    allow_caps = ["sys_admin", "net_admin", "net_bind_service"]

    volumes {
      enabled = true
    }
  }
}
